
<body>
<tbody>
  <tr>
    <a href="<%= new_gossip_path%>" class="btn btn-success">Créer un nouveau potin</a> 
  </tr>
</tbody>

<table>

  <br>
<div class="row justify-content-center">

  <div class="card-body">
    <%@gossips.each do |gossip|%>
    <h5 class="card-title"><%= gossip.title %></h5>
    <a href="<%= gossip_path(gossip.id)%>" class="btn btn-default"> <%= gossip.user.first_name %> </a><br>
    <a href="<%= edit_gossip_path(gossip.id)%>" class="btn btn-primary">Editer</a>
    <a href="<%= gossip_path(gossip.id)%>" class="btn btn-danger" data-confirm="Etes vous-sûr ?" data-method="DELETE">Supprimer</a>
  
    <% end %>
   
    </div>
</div>
</table>
</body>
